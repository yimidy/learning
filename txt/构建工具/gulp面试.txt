1. 介绍gulp？
	代码的自动化构建工具；对网站资源进行优化（合并，压缩），并且在开发过程中能够自动完成很多重复的任务（如编译文件，浏览器自动刷新），大大提高我们的工作效率。

2. 与grunt的不同之处？
	gulp是基于流的构建系统，使用代码由于配置的策略

		代码优于配置：维护gulp更像是在写代码，遵循CommonJs规范
	没有中间文件的生成
		使用Gulp的优势就是利用流的方式进行文件的处理，通过管道将多个任务和操作连接起来，因此只有一次I/O的过程，流程更清晰，更纯粹。Gulp去除了中间文件，只将最后的输出写入磁盘，整个过程因此变得更快。