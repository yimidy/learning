任何一个事件，初始都不是单单作用在一个元素上，还有包裹它的容器，或是整个页面，这就是事件流概念。

事件流描述的是从页面中接受事件的顺序。

	事件冒泡------------------从触发节点开始，向上冒泡，知道document（或window）
		
	事件捕获------------------从根节点开始（document或window）开始，向下捕获，直到触发节点


DOM事件流
	
	事件捕获---处于目标阶段---事件冒泡（事件冒泡阶段包括处于目标阶段）